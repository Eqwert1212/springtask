spring.datasource.url=${TEST_DB_URL:jdbc:postgresql://localhost:5433/springtask_test}
spring.datasource.username=${TEST_DB_USER:${DB_USER:springtask}}
spring.datasource.password=${TEST_DB_PASSWORD:${DB_PASSWORD:springtask_local}}
